name: sapar

services:
  # Mongo is OPT-IN / legacy. The application uses PostgreSQL (Prisma) as its
  # sole datastore. Start Mongo only if you specifically need it:
  #   docker compose --profile mongo up -d     (and set MONGO_URI in docker/.env)
  mongo:
    image: mongo:7
    profiles: ["mongo"]
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:-sapar}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-sapar}
      MONGO_INITDB_DATABASE: ${MONGO_INITDB_DATABASE:-sapar}
    volumes:
      - sapar-mongo-data:/data/db
    networks:
      - sapar
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - sapar-pg-data:/var/lib/postgresql/data
    networks:
      - sapar
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  api:
    build:
      context: ../sapar-typescript-backend
      dockerfile: Dockerfile
    image: sapar-api:slice-0
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: ${PORT}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN:-}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      TS_NODE_TRANSPILE_ONLY: "true"
      POSTGRES_USER: ${POSTGRES_USER:-sapar}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sapar_local_password}
      DATABASE_URL: ${DATABASE_URL}
      # Entrypoint runs prisma migrate deploy + idempotent baseline seed on boot.
      SEED_ON_BOOT: ${SEED_ON_BOOT:-true}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      # AES-256-GCM key (32-byte hex) for encrypting stored BYOK provider keys
      # (cluster H). If unset the app falls back to deriving a key from
      # JWT_SECRET (with a warning) — set this in production so encrypted keys
      # survive JWT_SECRET rotations. Generate with: openssl rand -hex 32
      AI_ENCRYPTION_KEY: ${AI_ENCRYPTION_KEY:-}
      # Per-user daily AI call quota (cluster H, slice H.4). Defaults to 200.
      AI_MAX_CALLS_PER_DAY: ${AI_MAX_CALLS_PER_DAY:-}
      WHATSAPPCRM_API_KEY: ${WHATSAPPCRM_API_KEY}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM: ${SMTP_FROM}
      REDIS_URL: ${REDIS_URL:-}
    volumes:
      - sapar-uploads:/app/uploads
    networks:
      - sapar
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: ../sapar-typescript-frontend
      dockerfile: Dockerfile
      args:
        VITE_DEMO_MODE: ${VITE_DEMO_MODE:-false}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    image: sapar-web:slice-0
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
    networks:
      - sapar
    depends_on:
      - api

  # ===========================================================================
  # Opt-in services (use:  docker compose --profile redis up -d --build)
  # ===========================================================================

  redis:
    image: redis:7-alpine
    profiles: ["redis"]
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - sapar-redis-data:/data
    networks:
      - sapar
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  worker:
    profiles: ["redis"]
    build:
      context: ../sapar-typescript-backend
      dockerfile: Dockerfile
    image: sapar-api:slice-0
    restart: unless-stopped
    # In Slice 0 the worker container is a placeholder: it boots the same image
    # but only runs the in-process cron registration (no separate BullMQ
    # consumer yet -- that lands in Slice 1.7).
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: 3002
      JWT_SECRET: ${JWT_SECRET}
      MONGO_URI: ${MONGO_URI:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      REDIS_URL: ${REDIS_URL}
    networks:
      - sapar
    depends_on:
      redis:
        condition: service_healthy

  # Automated PostgreSQL Backup cron (daily, keeps 7 days + 4 weeks + 6 months)
  db-backup:
    image: prodrigestivill/postgres-backup-local:16
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      SCHEDULE: "@daily"
      BACKUP_KEEP_DAYS: 7
      BACKUP_KEEP_WEEKS: 4
      BACKUP_KEEP_MONTHS: 6
    volumes:
      - sapar-backups:/backups
    networks:
      - sapar
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  sapar-mongo-data:
  sapar-pg-data:
  sapar-redis-data:
  sapar-uploads:
  sapar-backups:

networks:
  sapar:
    driver: bridge
